name: counter
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0-140.0.dev

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  vector_math: ^2.1.4

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

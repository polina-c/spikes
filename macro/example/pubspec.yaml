name: example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0-36.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  disposable_macro:
    path: ../disposable_macro

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


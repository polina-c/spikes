name: macro_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0-18.0.dev <4.0.0'

dependencies:
  macros: ^0.1.0-0
  flutter:
    sdk: flutter
  macro_disposable:
    path: ../macro_disposable

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
